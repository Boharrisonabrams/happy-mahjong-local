# Production Environment Configuration for Vercel

# Database - MUST be updated with actual production database
DATABASE_URL=your-production-database-url-here

# Production Server Configuration
NODE_ENV=production
PORT=3000

# Session Secret - MUST be updated with secure production secret
SESSION_SECRET=your-production-session-secret-here

# Production Mode - disable local dev features
LOCAL_DEV_MODE=false
DISABLE_REPLIT_AUTH=false

# File Storage Configuration for Production
# Update these for production file storage solution
PUBLIC_OBJECT_SEARCH_PATHS=/uploads/public
PRIVATE_OBJECT_DIR=/uploads/private

# Feature Flags for Production
ANALYTICS_ENABLED=true
BILLING_ENABLED=false

# Production Authentication (if using Replit Auth)
REPLIT_DOMAINS=your-production-domain.vercel.app
ISSUER_URL=https://replit.com/oidc
REPL_ID=your-production-repl-id